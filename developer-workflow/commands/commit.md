---
description: "Commit changes in meaningful chunks with best practices, protecting sensitive files"
argument-hint: "[message] - optional commit message or scope hint"
---

Use and follow the `git-commit-helper` skill exactly as written.

If user provided arguments: `$ARGUMENTS` - use as hint for commit scope/message.
